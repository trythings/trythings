{
  "name": "ellies-pad",
  "version": "0.0.0",
  "description": "Ellie's Pad",
  "main": "src/index.js",
  "private": true,
  "license": "private",
  "scripts": {
    "lint": "eslint src",
    "start": "env NODE_ENV=development webpack-dev-server --content-base static --history-api-fallback --host 0.0.0.0 --inline --output-public-path http://localhost:3000/static/ --port 3000",
    "build": "env NODE_ENV=production webpack --bail",
    "test": "npm run lint"
  },
  "repository": {
    "type": "git",
    "url": "github.com/trythings/trythings"
  },
  "author": "Try Things"
}
